# CLAUDE.md — キャスト管理システム「C-girl」

Claude Code が毎セッション最初に読む入口ファイル。**実装前に必ず `docs/` を読むこと。**

## プロジェクト概要
ガールズバー/コンカフェ「C-girl」向けキャスト管理システム。紙・Excel・電話の業務をWeb化し、
給与計算の自動化・機微情報の漏洩防止・経営の見える化を実現する。開発元：合同会社 Y&Y。
現在は **提案書＋UIモックのみが情報源**で、給与の具体値など未確定（★）が多い。

## まず読む資料（docs/）
| ファイル | 内容 |
|---|---|
| 00_README.md | 全体インデックスと現状 |
| 01_要件定義書.md | 機能(A/C-xx)・非機能・スコープ。**A-10=設定マスタ管理** |
| 02_設計指針書.md | アーキ・技術スタック・マルチ店舗・セキュリティ方針 |
| 03_給与計算仕様書.md | 給与ロジック（マスタ駆動・★は設定画面で店が入力） |
| 04_データモデル設計.md | マルチ店舗対応の論理モデル・ER |
| 05_要確認事項リスト.md | 店側ヒアリングで埋める空欄（A群＝着手前に必須） |
| 06_ClaudeCode開発ガイド_プロンプト集.md | フェーズ分け・Opus/Sonnet使い分け・各タスクのプロンプト |
| 07_サンプル実装プロンプト.md | 現有資料で作るデモの実装プロンプト |
| 08_設定画面_項目定義.md | 管理者設定画面の入力フォーム定義 |

## 絶対原則（守る）
1. **マスタ駆動：** 時給・単価・率・控除・締め日・pt定義などの数値はコードに固定しない。
   必ず設定（マスタ）から参照し、管理者が画面で変更できる（01 A-10 / 03 / 08）。
2. **権限分離：** キャストは自分のデータと公開ランキングのみ。他者の給与・本名・売上は一切不可（IDOR厳禁）。
3. **マルチ店舗前提：** 全主要テーブルに store_id。クエリは所属店舗でスコープ（表示は1店舗固定）。
4. **給与は単一エンジン：** 計算は1モジュール（payroll）に集約。再計算可能・監査可能。確定はスナップショットで不変。
5. **★未確定値はハードコードしない：** 仮値はseed/設定に置き、後で差し替え可能に。
   給与エンジンの本実装は 05 のA群が確定するまで保留（サンプル/ガワまではOK）。

## モデル運用（チーム）
- **Opus＝決める/守る/疑う：** 設計判断・給与ロジック・セキュリティ・PRレビュー・独立検証。
- **Sonnet＝作る/量産：** 画面実装・CRUD・Excel取込・テスト・定型作業。
- 各フェーズ末にOpusの独立レビュー（特に給与・権限）。並列は依存の無い独立タスクに限る。

## いま着手するもの
現有資料での **サンプル（デモ用プロトタイプ）**。手順は 07 と 08 に従う。
- スタック（サンプル）：React + TypeScript + Vite + Tailwind、状態はContext。`npm install && npm run dev` で起動。
- 目玉：設定画面で時給/単価を変更 → キャストの給与明細が再計算される。
- デモなので実DB・本番認証は作らない（READMEに明記）。

## ディレクトリ規約（サンプル）
```
src/
  pages/        画面（login, cast/*, admin/*）
  components/   UI部品
  store/        settings.ts（設定マスタ）, auth context
  lib/          payroll.ts（給与計算）, ranking.ts（集計）
  data/         seed.ts（キャスト/実績の初期データ）
  config/       brand（色#c8243e・タイポ等のデザイントークン）
docs/           本仕様一式
```

## コミット/作法
- 変更は小さく、意味単位で。仕様に関わる判断は docs/adr/ にADRとして残す。
- 数値・ロジックを変えたらテストを更新。給与・権限は必ずテストを伴う。
